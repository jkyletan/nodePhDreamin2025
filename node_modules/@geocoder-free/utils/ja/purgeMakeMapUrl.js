"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = purgeMakeMapUrl;

var _removeSpace = _interopRequireDefault(require("../lib/removeSpace"));

var _purgeAddress = _interopRequireDefault(require("./purgeAddress"));

var _cutToBlockNumber = _interopRequireDefault(require("./cutToBlockNumber"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function purgeMakeMapUrl(address, prefix, mapBaseUrl, fDelSpace = false) {
  const addressAft = (0, _purgeAddress.default)(address);
  let addressForMap = `${prefix || ''}${(0, _cutToBlockNumber.default)(addressAft)}`;

  if (fDelSpace) {
    addressForMap = (0, _removeSpace.default)(addressForMap);
  }

  const result = `${mapBaseUrl}${encodeURIComponent(addressForMap)}`;
  return result;
}

module.exports = exports.default;