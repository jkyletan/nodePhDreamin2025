"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getLatLngByDecimalStr;

/**
 * get latitude&longtitude array from decimal latlng string
 *
 * '35.123, 149.123' -> [35.123, 149.123]
 *
 * returns null if no matches found
 *
 * @param {*string} deciamlStr
 */
function getLatLngByDecimalStr(deciamlStr) {
  if (deciamlStr == null) {
    return null;
  }

  const [lat, lng] = deciamlStr.split(',');

  if (lat == null || lng == null || Number.isNaN(+lat) || Number.isNaN(+lng)) {
    return null;
  }

  return [+lat, +lng];
}

module.exports = exports.default;