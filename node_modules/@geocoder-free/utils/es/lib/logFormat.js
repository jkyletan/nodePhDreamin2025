function logFormat(msg, func) {
  const formattedMsg = `${new Date().toLocaleString()} -- ${msg}`;

  if (func != null) {
    func(formattedMsg);
  }

  return formattedMsg;
}

export default logFormat;